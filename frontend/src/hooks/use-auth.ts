// Re-exported from `lib/auth/auth-provider` so consumers can import from a
// conventional `hooks/` path without needing to know the provider lives
// under `lib/auth/`.
export { useAuth, useRole } from "@/lib/auth/auth-provider";
